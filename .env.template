# Generate secret key thus:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY = "SECRET KEY HERE"

DEBUG = True

# Comma-separated list of hostnames this instance is served from, e.g.
# "testomatic-01.local,testomatic-01"
ALLOWED_HOSTS = ""

# Register instance this device talks to, and an API key minted for it
# (see Register's authuser.User.regenerate_api_key / crm.views.users.user_edit).
REGISTER_API_URL = "https://register.example.com"
REGISTER_API_KEY = ""
